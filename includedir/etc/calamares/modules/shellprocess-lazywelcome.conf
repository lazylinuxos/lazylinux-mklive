---
dontChroot: false

timeout: 10

script:
    - command: "mkdir -p /home/${USER}/.config/autostart && cp /usr/share/applications/lazy-welcome.desktop /home/${USER}/.config/autostart/ && chown -R ${USER}:${USER} /home/${USER}/.config"

i18n:
    name: "Add LazyLinux welcome app to startup"
